package cd.software.flowchat.model

data class ChannelListEntry(val name: String,
                            val userCount: Int,
                            val description: String // Descripción opcional del canall
)
